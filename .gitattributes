# The nine code-provenance frozen files are hash-bound into Phase 2 execution manifests
# (phase2_execution.CODE_PROVENANCE_FROZEN_FILES). Their working-tree bytes must equal the
# committed git blobs byte-for-byte, so core.autocrlf smudging must never rewrite them.
# Scope is deliberately restricted to exactly these files: other files (notably uv.lock,
# whose raw hash is also manifest-bound) keep their current working-tree byte state.
rejudge/phase2_preflight_runner.py text eol=lf
rejudge/api_client.py text eol=lf
rejudge/phase2_execution.py text eol=lf
rejudge/phase2_role_limits.py text eol=lf
rejudge/phase2_capability_corpus.py text eol=lf
rejudge/phase2_prompt_bundle.py text eol=lf
rejudge/phase2_plan.py text eol=lf
rejudge/runner.py text eol=lf
rejudge/run_manifest.py text eol=lf

# The canary code-provenance set, hash-bound into the canary execution manifest. Deliberately
# a SEPARATE list from the nine above: adding a file to that tuple would change the
# capability preflight's own code_bundle_sha256 and force its manifest to be rebuilt.
# Several of these files execute the science but were previously hash-bound nowhere.
# All were verified LF in index and working tree when pinned, so the pin changes no bytes.
rejudge/judge_loop.py text eol=lf
rejudge/debate_gen.py text eol=lf
rejudge/composer.py text eol=lf
rejudge/oracle_channel.py text eol=lf
rejudge/query_screen.py text eol=lf
rejudge/parsers.py text eol=lf
rejudge/records.py text eol=lf
rejudge/config.py text eol=lf
rejudge/phase2_query_gate.py text eol=lf
rejudge/phase2_dual_gate.py text eol=lf
rejudge/phase2_canary_gate.py text eol=lf
rejudge/phase2_call_cache.py text eol=lf
rejudge/phase2_caching_client.py text eol=lf
rejudge/phase2_canary_cells.py text eol=lf
rejudge/phase2_canary_compose.py text eol=lf
rejudge/phase2_canary_execute.py text eol=lf
rejudge/phase2_canary_fixtures.py text eol=lf
rejudge/phase2_canary_order.py text eol=lf
rejudge/phase2_canary_runner.py text eol=lf
