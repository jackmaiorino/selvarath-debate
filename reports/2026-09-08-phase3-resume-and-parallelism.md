# Phase 3 crash recovery and provider parallelism

Jack authorized preserving completed work and replacing blanket restart-on-crash on September 8 UTC: "yea lets do that. restarting the entire experiment because of a single crash is not smart. Also ensure we've parallelized this as much as possible since its surprising its taking so long". This operational amendment supersedes the no-resume restriction for an authenticated recovery of the same scientific run. The original protocol, launch manifest and authorization remain preserved; the recovery signature binds the amended execution code and checkpoint. The aggregate cap remains USD 1,100.

The interrupted run is `phase3-main-6ca5e8e2cd156276`, with 2,131 of 9,840 judgments completed. Its last provider write was September 7 at 22:31:29 UTC, about three seconds before Windows forcibly closed Codex for an update. The timing supports that cause; Windows did not identify the Python processes directly. Recovery preserves the existing result, response journal, reviewer decisions, usage history and original run identity. It restores reviewer reservations, pass counters and the exact saved responses instead of generating them again. A separate process lease prevents duplicate runners.

The one unobserved interrupted request retains its full USD 0.11049000 liability as an unknown charge. Its identical request can be retried with the original seed and upstream responses under the amended transport recovery procedure. A retry can produce a different response; it is not described as exact replay. Already journaled responses, including empty responses, are reused byte-for-byte. Recovery also handles a crash after journaling a response but before clearing its marker without another request or charge. Corrupt histories and settled successes lacking saved responses require diagnosis. No result is selected according to its outcome.

Before continuation, cumulative exposure is USD 211.54310874: USD 180.32855562 before this run, USD 28.71847752 successful charges, and USD 2.49607560 uncertain charges including the interrupted request. The restored client counts each amount once and reserves every concurrent request against both the aggregate cap and the existing uncertainty ceiling. The original full-run forecast already covers this run's completed portion, so continuation does not incur the cost of repeating all 9,840 judgments.

Measured history attributes 93.4% of elapsed time to provider attempt intervals and 0.76% to reviewer waves. The provider driver and journal previously serialized network calls. Recovery uses eight cell workers, initially at most two Qwen and four Llama requests, with signed maxima of four per model. After 100 clean successful calls a model can advance to its maximum; an uncertain charge resets its limit and clean-call counter. Existing transport backoff, cooldowns and spending guards remain. Reviewer concurrency stays at 12. Together's [current rate-limit guidance](https://docs.together.ai/docs/billing-usage-limits) makes limits dynamic per model; concurrency increases are therefore measured rather than assumed to yield a fixed speedup.

Offline verification compares exact provider requests, seeds, parameters and full canonical results across serial and parallel execution for 120 real Phase 3 cells spanning all conditions. Focused tests cover duplicate-request exclusion, concurrent spending reservations, interrupted accounting, deterministic replay, recovered review commits, counters, per-model limits and provider provenance with interleaved calls. A fresh one-seed harness and read-only live preflight precede continuation. The launch record holds the final commit, signed recovery manifest, validation evidence and process IDs.

The independent Windows launcher survives the tested caller termination, runs outside Codex's process job and captures logs and exit status. The 30-minute monitor now performs authorized recovery, checks real processes and durable progress, and reports meaningful changes. Reboot or logoff survival is not claimed. Scientific models, prompts, questions, seeds, integer gates, terminal-disposition bounds and analysis remain unchanged. The older voided runs stay excluded; this amendment preserves only the current interrupted measurement.
